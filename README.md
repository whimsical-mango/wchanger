The script gets a random wallpaper from a [wallhaven.cc](https://wallhaven.cc) public collection and sets as a backgroud.

The usage is very simple: `python wchanger.py username/collectionID` or `wchanger.exe username/collectionID`
